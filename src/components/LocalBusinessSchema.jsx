/**
 * LocalBusinessSchema — TBB Reklam Yasağı Yönetmeliği Madde 7/e gereği
 * arama motoru optimizasyonu odaklı yapılandırılmış veri (priceRange, knowsAbout,
 * areaServed, fake review markup vb.) yasak kapsamına girdiği için bu bileşen
 * etkisiz hale getirilmiştir.
 *
 * Hiçbir yerde import edilmiyor; geriye dönük uyumluluk için boş bir bileşen olarak bırakıldı.
 */
const LocalBusinessSchema = () => null;

export default LocalBusinessSchema;
